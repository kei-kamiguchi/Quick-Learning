<div class="parts-show">
  <div class="container main-container animated fadeIn">
    <h3 class="main-title"><%= @part.subject.title %></h3>
    <div class="pagination-area">
      <% if @part.previous.present? %>
      <%= link_to "#{t '.prev'}", part_path(@part.previous), class: "btn pagination-btn btn-sm"%>
      <% end %>
      <% if @part.next.present? %>
      <%= link_to "#{t '.next'}",part_path(@part.next), class: "float-right btn pagination-btn btn-sm" %>
      <% end %>
    </div>
    <table class="table table-responsive">
      <thead class="thead-default">
        <tr>
          <th class="part-title"><%= @part.title %></th>
          <% if @achieved.present? %>
          <th class="achieved-icon text-right"><p><%= t '.achieved' %></p></th>
          <% else %>
          <th colspan="1"></th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td colspan="2" class="text-right">
            <% if user_signed_in? %>
            <%= link_to "#{t '.ask_a_question'}", new_part_question_path(@part), class: 'btn btn-outline-info btn-sm part-question' %>
            <% else %>
            <%= link_to "#{t 'attributes.edit'}", edit_part_path(@part), class: 'btn btn-outline-info btn-sm' %>
            <% end %>
          </td>
        </tr>
        <tr>
          <td colspan="2"><%= @part.content.html_safe %></td>
        </tr>
        <tr>
          <td colspan="2" class="text-right">
            <% if user_signed_in? %>
            <% if @achieved.present? %>
            <%= link_to "#{t '.finished_part'}", achieved_path(id: @achieved.id), method: :delete, class: 'btn btn-outline-info btn-sm mr-2' %>
            <% else %>
            <%= link_to "#{t '.unfinished_part'}", achieveds_path(part_id: @part.id), method: :post, class: 'btn btn-outline-info btn-sm mr-2' %>
            <% end %>
            <%= link_to "#{t '.ask_a_question'}", new_part_question_path(@part), class: 'btn btn-outline-info btn-sm part-question' %>
            <% else %>
            <%= link_to "#{t 'attributes.edit'}", edit_part_path(@part), class: 'btn btn-outline-info btn-sm' %>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
    <div class="pagination-area">
      <% if @part.previous.present? %>
      <%= link_to "#{t '.prev'}", part_path(@part.previous), class: "btn pagination-btn btn-sm"%>
      <% end %>
      <% if @part.next.present? %>
      <%= link_to "#{t '.next'}", part_path(@part.next), class: "float-right btn pagination-btn btn-sm" %>
      <% end %>
    </div>
  </div>
