<%= form_with(model: [test, test_question], local: true) do |form| %>
<div class="form-group row">
  <%= form.text_field :title, placeholder: "input title", class: "form-control col-11" %><%= form.text_area :content, placeholder: "input content", class: "form-control col-11 mt-1" %><%= form.select :form_size, [1,2,3], {},
  class: 'sample' %><%= form.submit nil, class: "btn btn-info btn-sm col-2 mt-1 ml-1"  %>
</div>
<% end %>
