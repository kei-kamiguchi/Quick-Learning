<% if answer_paper.edit == "edit" %>
<% if answer_paper.advice.present? %>
<div class="test-question-content">
  <p class="text-advice">アドバイス</p>
  <%= simple_format(h(answer_paper.advice), {}, sanitize: false, wrapper_tag: "div") %>
</div>
<% end %>
<div class="action text-right">
  <%= link_to answer_paper.edit, answer_paper_toggle_edit_path(answer_paper), method: :patch, remote: true, class: "btn btn-outline-info btn-sm" %>
</div>
<% else %>
<div class="test-question-content">
<%= form_with model: answer_paper do |form| %>
<%= form.text_area :advice, rows:5, class: "form-control" %>
<div class="text-right action">
  <%= form.submit nil, class: 'btn btn-info btn-sm mt-1' %>
</div>
<% end %>
</div>
<% end %>
